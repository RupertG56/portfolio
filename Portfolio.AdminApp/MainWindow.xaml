<Window x:Class="Portfolio.AdminApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Portfolio.AdminApp"
		xmlns:vm="clr-namespace:Portfolio.AdminApp.ViewModel"
		xmlns:controls="clr-namespace:Portfolio.AdminApp.Controls"
        mc:Ignorable="d"
		d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        Title="{Binding Title}" Height="450" Width="800">
	<Window.Resources>
		<!-- Framework converter used to swap controls dynamically -->
		<BooleanToVisibilityConverter x:Key="BoolToVis" />
		<!-- The implicit DataTemplate links the ViewModel to the UserControl -->
		<DataTemplate DataType="{x:Type vm:SiteViewModel}">
			<controls:SiteControl />
		</DataTemplate>
	</Window.Resources>
	<Grid>
		<ContentControl Content="{Binding SiteViewModel}" />
    </Grid>
</Window>
